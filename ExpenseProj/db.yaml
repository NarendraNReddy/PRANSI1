- name: configure db
  hosts: db
  become: yes 
  tasks:
  - name: install mysql-server
    ansible.builtin.dnf:
      name: mysql-server
      state: latest
  - name: enble and start mysqld
    ansible.builtin.service:
      name: mysqld
      state: started
      enabled: yes
          