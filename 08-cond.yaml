- name: cond check
  hosts: localhost
  become: no 
  tasks:
  - name: cond check
    ansible.builtin.command: id expense4
    ignore_erros: true
    register: USER
  
    

  - name: Hello
    ansible.builtin.debug:
      msg: "Hello"  